#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    double a,b,c;

    cin >> a >> b >> c;

    double media = ((a*2) + (b*3) + (c*5))/10.0;

    cout << setprecision(1) << fixed;
    cout << "MEDIA = " << media << endl;
        
    return 0;
}
