#include <bits/stdc++.h>
using namespace std;
#define bieo cin.tie(0)->sync_with_stdio(0)
#define int long long
#define endl '\n'

void solve() {
    int n;
    cin >> n;
    vector<int> p(n), s(n-1), c(n);
    for(auto &x : p) cin >> x;
    for(auto &x : s) cin >> x;
    
}

signed main() {
    bieo;
    int t = 1;
    // cin >> t;
    while (t--) solve();
    return 0;
}