#include <bits/stdc++.h>
#define int long long
const int MAXX = 1e17;
using namespace std;

signed main (){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--)
    {
        int w, f, n, curF = 0, curW = 0;

        cin >> w >> f >> n;

        vector<int> monsters(n);
        for (auto &x : monsters) cin >> x;

        
    }

    return 0;
}