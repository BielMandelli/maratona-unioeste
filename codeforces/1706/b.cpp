#include <bits/stdc++.h>
#define int long long
using namespace std;

signed main(){
    int t;
    cin >> t;

    while(t--){
        int n;
        cin >> n;
        vector<int> colors(n);
        vector<vector<bool>> active

        for (auto &x: colors) cin >> x;


    }
    return 0;
}